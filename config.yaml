paths:
  raw_data: data/raw/
  processed_data: data/processed/
  models: models/

model:
  type: lightgbm
  params:
    n_estimators: 500
    learning_rate: 0.05
    max_depth: -1

training:
  test_size: 0.2
  random_state: 42
