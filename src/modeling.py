import pandas as pd

def train_models(df_train, df_test):
    """
    Train the specified models on the specified dataframe.

    Parameters
    ----------
    df : pd.DataFrame
        The dataframe to train the models on.

    Returns
    -------
    dict
        The trained models.
    """
    models = {}
    return models   