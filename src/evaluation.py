import pandas as pd
def evaluate_models(models, df_test):
    """
    Evaluate the specified models on the specified dataframe.

    Parameters
    ----------
    models : dict
        The trained models.
    df_test : pd.DataFrame
        The test dataframe.

    Returns
    -------
    dict
        The evaluation metrics for each model.
    """
    metrics = {}
    return metrics
    